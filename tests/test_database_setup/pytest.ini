[pytest]
addopts = -q -m "not api and not euhealth and not slow"
testpaths = tests
markers =
  slow: downloads models or hits network
  euhealth: requires a built euhealth.db (and optional network for deepdive)
  api: tests that hit real external APIs (Azure/OpenAI/HF/Local)
